find_program(XEMU NAMES xmega65 xmega65.native xmega65.exe xemu-xmega65 xemu-xmega65.exe)
set(XEMU_ARGS "-headless -sleepless -testing -model 3 -hyperserialascii -hyperdebug")

function(TEST NAME)
    add_executable(${NAME} ${NAME}.c)
    target_link_libraries(${NAME} mega65libc)
    set_target_properties(${NAME} PROPERTIES OUTPUT_NAME ${NAME}.prg)
    target_compile_options(${NAME} PRIVATE -Os ${CLANG_WARNINGS})
    if (XEMU)
        add_test(NAME ${NAME} COMMAND sh -c "${XEMU} ${XEMU_ARGS} -prg $<TARGET_FILE:${NAME}>")
    endif()
endfunction()

TEST(test-math)
TEST(test-fileio)
TEST(test-integer-size)
TEST(test-memory)
TEST(test-time)
